import FlexContainer from "design-system/FlexContainer";
import { FlexContainer as DesignedFlexContainer } from "design-system-types";

const FlexContainerComponent = FlexContainer as DesignedFlexContainer;

export default FlexContainerComponent;
