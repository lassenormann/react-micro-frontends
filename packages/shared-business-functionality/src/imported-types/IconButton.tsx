import IconButton from "design-system/IconButton";
import { IconButton as DesignedIconButton } from "design-system-types";

const IconButtonComponent = IconButton as DesignedIconButton;

export default IconButtonComponent;
